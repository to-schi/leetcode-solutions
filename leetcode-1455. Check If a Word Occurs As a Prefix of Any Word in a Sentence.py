def isPrefixOfWord(self, sentence: str, searchWord: str) -> int:
    sentence
    searchWord

    # for word in sentence:
    #     while pre != word[0:len(pre)]:
    #         pre = pre[0:(len(pre)-1)]
    #         #plen -= 1
    #         if len(pre) == 0:
    #             return ""
    # return pre

print(isPrefixOfWord(1, "i love eating burger", "burg"))
